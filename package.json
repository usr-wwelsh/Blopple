{
  "name": "blopple",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "editor:dev": "bun run --filter=@blopple/editor dev",
    "editor:build": "bun run --filter=@blopple/editor build",
    "runtime:build": "bun run --filter=@blopple/runtime build"
  }
}
